import './globals.css'
export const metadata = { title: 'Email Triage Agent', description: 'AI-powered email management' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
